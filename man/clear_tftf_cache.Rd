% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cache.R
\name{clear_tftf_cache}
\alias{clear_tftf_cache}
\title{Clear the TFTF data cache}
\usage{
clear_tftf_cache(cache_dir = NULL, pattern = NULL, quiet = FALSE)
}
\arguments{
\item{cache_dir}{Optional cache directory to clean; default \code{NULL} uses the currently active cache directory.}

\item{pattern}{Optional regular expression; when given, only cached files whose name matches are removed.}

\item{quiet}{Logical, whether to suppress the summary message.}
}
\value{
Number of removed files (invisibly).
}
\description{
Remove cached .rds files (and the in-memory cache) produced by \code{get_data()} and \code{pantissue_cor_analysis()}. This can be useful when the underlying databases have been updated and you want the data to be downloaded again (otherwise use \code{refresh = TRUE} on the fetching function).
}
\examples{
\dontrun{
clear_tftf_cache()
clear_tftf_cache(pattern = "hTFtarget")
}
}
